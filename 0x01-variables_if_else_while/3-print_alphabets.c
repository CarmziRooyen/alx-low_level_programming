#include <stdio.h>
/**
 * main - Main block
 *Description: Print alphabet in lower case
 * Return: Always 0
 */
int main(void)
{
	char c = 'a';

	while (c <= 'z')
	{
		putchar(c);
		c++
	}
	c = 'A';

	while (c <= 'Z')
		putchar(C);
		C++
	}

	putchar('\n');
	return (0);
}
